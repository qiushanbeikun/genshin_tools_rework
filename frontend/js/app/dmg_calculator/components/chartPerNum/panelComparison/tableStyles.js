import { makeStyles } from '@mui/styles';

export const tableStyles = makeStyles(() => ({
  tableHead: {
    backgroundColor: '#ebebeb',
  },
  relDmg: {
    color: '#c72727',
    fontWeight: 500,
  },
}));
